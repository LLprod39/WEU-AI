# Changelog: Детальное логирование Claude CLI

## Дата: 2026-01-31

## Описание

Добавлено детальное логирование для отладки запуска и выполнения Claude агента через CLI.

## Изменения в коде

### 1. `agent_hub/views.py` - Функция `_run_cli_stream`

**Добавлено:**
- 🔍 Детальная отладочная информация перед запуском процесса:
  - Полная команда с каждым аргументом
  - Все переменные окружения (HOME, PATH, USER, SHELL, custom ENV)
  - Содержимое MCP конфига если он используется
  - Проверка существования MCP конфига

- 🚀 Логирование запуска процесса:
  - Информация о запуске subprocess.Popen
  - PID процесса
  - Ошибки при запуске с детальным описанием

- 📖 Логирование каждой строки вывода:
  - Номер строки
  - Полное содержимое строки (с ограничением 200 символов для длинных строк)
  - Парсинг JSON ответов с выделением типа и подтипа
  - Детальная информация о tool_call, assistant response, errors

- 🏁 Логирование завершения процесса:
  - Exit code
  - Количество строк вывода
  - Размер вывода в символах
  - Количество tool вызовов
  - Анализ причины ошибки для non-zero exit codes:
    - exit code -9: SIGKILL (нехватка памяти)
    - exit code -15: SIGTERM
    - exit code 1: Общая ошибка
    - exit code 127: Команда не найдена
  - Последние 10 строк вывода при ошибке

- 🖨️ Дублирование критичных логов через print():
  - Запуск процесса
  - Каждая строка вывода
  - JSON ответы
  - Exit code
  - Ошибки

### 2. `agent_hub/views.py` - Функция `_run_steps_with_backend`

**Добавлено:**
- 🚀 Детальное логирование перед запуском агента:
  - Step title и label
  - Runtime и model
  - Workspace path
  - Ralph iteration / max iterations
  - Retry attempt / max retries
  - Полная конфигурация (config)
  - Промпт (первые 500 символов)
  - Полная CLI команда

- 🧪 Тест работоспособности CLI (только первый раз):
  - Проверка существования CLI файла
  - Проверка прав на выполнение
  - Запуск с --version для проверки работоспособности

- 🔎 Анализ промпта:
  - Проверка наличия упоминания сервера ("prod server", IP)
  - Проверка наличия инструкций об использовании server_execute
  - Проверка наличии метки серверной задачи

- 📊 Детальное логирование результата выполнения:
  - Success/failure статус
  - Exit code
  - Длина вывода
  - Первые 1000 символов вывода
  - Последние 500 символов вывода
  - Детальный анализ ошибок с типом проблемы

### 3. `agent_hub/views.py` - Функция `_build_cli_command`

**Добавлено:**
- 🔧 Детальное логирование построения команды:
  - Runtime и workspace
  - Ключи конфигурации
  - Длина промпта
  - Runtime config
  - Resolved command path
  - Base args и formatted args
  - Allowed args
  - Каждый добавленный аргумент с его значением
  - Prompt style
  - Финальная команда (количество элементов)

### 4. `agent_hub/views.py` - Функция `_resolve_cli_command`

**Добавлено:**
- 🔍 Детальное логирование разрешения CLI команды:
  - Runtime
  - ENV переменная для runtime
  - Значение ENV переменной
  - Проверка существования файла из ENV
  - Runtime config
  - Command из config
  - Проверка абсолютного пути
  - Результат shutil.which
  - Финальный resolved path

### 5. `agent_hub/views.py` - Функция `_format_arg`

**Добавлено:**
- Логирование форматирования аргументов (debug level)

## Новые файлы

### 1. `scripts/test_claude_cli.py`

Тестовый скрипт для диагностики Claude CLI:
- Проверка переменной окружения CLAUDE_CLI_PATH
- Поиск команды в PATH
- Проверка существования и прав на выполнение
- Запуск с --version
- Тест с минимальным промптом

### 2. `docs/DEBUG_CLAUDE_CLI.md`

Документация по отладке:
- Описание симптомов и причин ошибок
- Пошаговая диагностика
- Примеры команд для проверки
- Решения типичных проблем
- Интерпретация логов

## Использование

### 1. Запуск workflow

Теперь при запуске workflow через Agent Hub вы увидите детальные логи в консоли:

```bash
# Смотрим логи Docker контейнера
docker logs -f web_rA_web_1

# Или через журналы на хосте
tail -f logs/django.log
```

### 2. Поиск в логах

Ключевые маркеры для поиска:

```bash
# Детальная отладка запуска
grep "ДЕТАЛЬНАЯ ОТЛАДКА CLI" logs/django.log

# Запуск процесса
grep "Запуск процесса" logs/django.log

# Ошибки
grep "ОШИБКА" logs/django.log

# Exit code
grep "Exit code" logs/django.log

# JSON ответы от Claude
grep "JSON ответ" logs/django.log
```

### 3. Проверка Claude CLI

```bash
# Запустите тестовый скрипт
python scripts/test_claude_cli.py

# Или из Django shell
python manage.py shell
>>> from pathlib import Path
>>> import subprocess
>>> # ... запустите тесты вручную
```

## Примеры логов

### Успешный запуск

```
[DEBUG] 🚀 Запуск процесса: /home/lunix/.local/bin/claude
[DEBUG] 🎯 Полная команда: /home/lunix/.local/bin/claude -p --mcp-config /path/to/mcp.json "Check disk space"
[DEBUG] ✅ Процесс запущен успешно, PID: 12345
[DEBUG] 📖 Начинаем читать вывод процесса PID=12345
[DEBUG] 📥 Строка #1: {"type":"assistant","message":{...}}
[DEBUG] 📦 JSON #1: type=assistant, subtype=
[DEBUG] 💬 Assistant: I'll check the disk space using server_execute...
[DEBUG] 📥 Строка #2: {"type":"tool_call","tool_name":"server_execute",...}
[DEBUG] 📦 JSON #2: type=tool_call, subtype=started
[DEBUG] 🔧 Tool call: server_execute
[DEBUG] 🏁 Exit code=0
[DEBUG] 📊 ЗАВЕРШЕНИЕ: exit_code=0, строк=25, размер=5432
```

### Ошибка exit code -9

```
[DEBUG] 🚀 Запуск процесса: /home/lunix/.local/bin/claude
[DEBUG] 🎯 Полная команда: /home/lunix/.local/bin/claude -p --mcp-config /path/to/mcp.json "Check disk space"
[DEBUG] ✅ Процесс запущен успешно, PID: 12345
[DEBUG] 📖 Начинаем читать вывод процесса PID=12345
[DEBUG] 📥 Строка #1: {"type":"assistant","message":{...}}
[DEBUG] 🏁 Exit code=-9
[DEBUG] ⚠️ ОШИБКА! exit_code=-9
INFO     | agent_hub.views:_run_cli_stream:1588 - 📊 ДЕТАЛИ ЗАВЕРШЕНИЯ ПРОЦЕССА:
  Exit Code: -9
  Всего строк вывода: 1
  Размер вывода: 234 символов
  Причина: Процесс был убит (SIGKILL) - возможно нехватка памяти
```

## Следующие шаги

1. Запустите workflow с Claude агентом
2. Проверьте логи в консоли
3. Если есть ошибки - используйте `scripts/test_claude_cli.py`
4. Смотрите `docs/DEBUG_CLAUDE_CLI.md` для решения проблем

## Уровни логирования

- **INFO**: Основные события (запуск, завершение, результаты)
- **DEBUG**: Детальная информация (каждая строка, аргументы)
- **ERROR**: Ошибки
- **WARNING**: Предупреждения

По умолчанию в консоли будут INFO и выше. Для DEBUG уровня настройте:

```python
# settings.py
LOGGING = {
    'loggers': {
        'agent_hub': {
            'level': 'DEBUG',
        }
    }
}
```

## Отключение детального логирования

Если нужно отключить детальное логирование:

1. Закомментируйте `print()` вызовы в `_run_cli_stream`
2. Измените `logger.info()` на `logger.debug()` для менее важных сообщений
3. Уберите тест CLI в `_run_steps_with_backend`
