cli:
  backend: "cursor"
event_loop:
  completion_promise: "LOOP_COMPLETE"
  max_iterations: 50
  starting_event: "task.start"
hats:
  step_1:
    name: "Проверить место на дисках"
    description: "Проверить место на дисках"
    triggers: ["task.start"]
    publishes: ["step_1.done"]
    instructions: "Execute via server_execute on server lunix: check disk space (df -h). Output <promise>STEP_DONE</promise> when done"