cli:
  backend: "cursor"
event_loop:
  completion_promise: "LOOP_COMPLETE"
  max_iterations: 50
  starting_event: "task.start"
hats:
  step_1:
    name: "Проверить место на диске"
    description: "Проверить место на диске"
    triggers: ["task.start"]
    publishes: ["step_1.done"]
    instructions: "Выполни на сервере lunix через server_execute команду проверки места на диске (df -h). Покажи результат и выведи <promise>STEP_DONE</promise> когда готово."