cli:
  backend: "cursor"
event_loop:
  completion_promise: "LOOP_COMPLETE"
  max_iterations: 50
  starting_event: "task.start"
hats:
  step_1:
    name: "Развернуть Grafana в Docker на порту 9500"
    description: "Развернуть Grafana в Docker на порту 9500"
    triggers: ["task.start"]
    publishes: ["step_1.done"]
    instructions: "Execute via server_execute on server lunix: разверни Grafana в Docker и настрои на порту 9500. Используй docker run или docker-compose. Убедись, что Grafana доступна на порту 9500. Output <promise>STEP_DONE</promise> when done"