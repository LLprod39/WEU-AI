# Структура проекта WEU AI Agent Platform

Детальное описание структуры проекта и назначения каждого компонента.

## 📂 Корневая структура

```
web_rA/
├── .gitignore              # Игнорируемые файлы Git
├── .env                    # Переменные окружения (не в репозитории)
├── .model_config.json      # Конфигурация моделей (не в репозитории)
├── README.md               # Основная документация проекта
├── PROJECT_STRUCTURE.md    # Этот файл - детальная структура
├── QUICK_START.md          # Быстрый старт
├── IMPLEMENTATION_SUMMARY.md # Сводка реализованных функций
├── GEMINI.md               # Документация по Gemini
├── requirements.txt        # Python зависимости
├── docker-compose.yml      # Docker Compose для Qdrant
├── main.py                 # Точка входа для NiceGUI приложения
├── manage.py               # Django management скрипт
├── start.bat               # Скрипт запуска для Windows
├── db.sqlite3              # SQLite база данных (не в репозитории)
├── test_simple.py          # Простые тесты
├── test_model_selection.py # Тесты выбора моделей
│
├── app/                    # Основная логика приложения
├── agent_hub/              # Django приложение: Управление агентами
├── core_ui/                # Django приложение: Основной UI
├── tasks/                  # Django приложение: Система задач
├── passwords/              # Django приложение: Пароль-менеджер
├── servers/                # Django приложение: Управление серверами
├── web_ui/                 # Настройки Django проекта
├── docs/                   # Документация
└── media/                  # Загруженные файлы и workflow
```

## 📁 Детальное описание директорий

### `/app` - Основная логика приложения

Ядро системы, содержащее всю бизнес-логику.

#### `/app/agents` - Система агентов

```
agents/
├── __init__.py
├── base_agent.py          # Базовый класс для всех агентов
├── simple_agent.py        # Простой агент для быстрых ответов
├── react_agent.py         # ReAct агент с Reasoning и Acting
├── complex_agent.py       # Сложный агент с планированием
├── ralph_agent.py         # Ralph Wiggum итеративный агент
├── cli_runtime.py         # CLI рантайм для внешних агентов
└── manager.py             # Менеджер агентов
```

**Назначение**: Реализация различных типов AI-агентов с разными стратегиями выполнения задач.

#### `/app/core` - Ядро системы

```
core/
├── __init__.py
├── llm.py                 # LLM Provider (Gemini, Grok)
├── model_config.py        # Управление конфигурацией моделей
└── orchestrator.py        # Оркестратор - координация компонентов
```

**Назначение**: 
- `llm.py`: Абстракция над API провайдеров (Gemini, Grok)
- `model_config.py`: Управление настройками моделей
- `orchestrator.py`: Координация работы RAG, инструментов и агентов

#### `/app/mcp` - Model Context Protocol

```
mcp/
├── __init__.py
└── client.py              # MCP клиент
```

**Назначение**: Интеграция с Model Context Protocol для расширенной функциональности.

#### `/app/rag` - RAG система

```
rag/
├── __init__.py
├── engine.py              # Основной RAG движок
└── inmemory_rag.py        # InMemory реализация RAG
```

**Назначение**: Retrieval-Augmented Generation система для улучшения ответов AI на основе загруженных документов.

#### `/app/tools` - Инструменты агентов

```
tools/
├── __init__.py
├── base.py                # Базовый класс для инструментов
├── manager.py             # Менеджер инструментов
├── filesystem_tools.py    # Инструменты для работы с файловой системой
├── ssh_tools.py           # Инструменты для SSH подключений
└── web_tools.py           # Инструменты для веб-скрапинга
```

**Назначение**: Набор инструментов, которые могут использовать агенты для выполнения задач.

#### `/app/ui` - NiceGUI компоненты

```
ui/
└── orchestrator_panel.py  # Панель оркестратора для NiceGUI
```

**Назначение**: UI компоненты для NiceGUI приложения (запускается через `main.py`).

#### `/app/utils` - Утилиты

```
utils/
├── __init__.py
└── file_processor.py      # Обработка загруженных файлов
```

**Назначение**: Вспомогательные утилиты, например, обработка файлов (PDF, DOCX, изображения с OCR).

---

### `/agent_hub` - Django приложение: Управление агентами

```
agent_hub/
├── __init__.py
├── admin.py               # Админ-панель Django
├── apps.py                # Конфигурация приложения
├── models.py              # Модели данных (AgentProfile, AgentPreset, AgentRunLog)
├── views.py               # Представления (веб-интерфейс)
├── urls.py                # URL маршруты
├── migrations/            # Миграции базы данных
│   ├── 0001_initial.py
│   ├── 0002_seed_presets.py
│   ├── 0003_agent_workflows.py
│   └── 0004_add_project_path.py
└── templates/
    └── agent_hub/
        └── agents.html    # Шаблон веб-интерфейса
```

**Назначение**: Управление профилями агентов, пресетами, workflow и логами выполнения.

**Модели**:
- `AgentProfile`: Профили агентов пользователей
- `AgentPreset`: Предустановленные конфигурации
- `AgentWorkflow`: Workflow для агентов
- `AgentRunLog`: Логи выполнения агентов

---

### `/core_ui` - Django приложение: Основной UI

```
core_ui/
├── __init__.py
├── admin.py
├── apps.py
├── models.py              # Модели (если есть)
├── views.py               # Основные представления (чат, оркестратор, настройки)
├── urls.py                # URL маршруты
├── tests.py
├── migrations/
├── static/
│   └── css/
│       └── style.css      # Стили CSS
└── templates/
    ├── base.html          # Базовый шаблон
    ├── chat.html          # Страница чата
    ├── orchestrator.html  # Страница оркестратора
    ├── knowledge_base.html # База знаний
    ├── settings.html      # Настройки
    └── login.html         # Страница входа
```

**Назначение**: Основной веб-интерфейс приложения - чат, оркестратор, настройки, база знаний.

**Основные страницы**:
- `/` - Главная страница (чат)
- `/orchestrator/` - Панель оркестратора
- `/knowledge-base/` - База знаний
- `/settings/` - Настройки
- `/login/` - Вход в систему

---

### `/tasks` - Django приложение: Система задач

```
tasks/
├── __init__.py
├── admin.py               # Админ-панель
├── apps.py
├── models.py              # Модели задач (Task, TaskLabel, TaskComment, etc.)
├── views.py               # Представления
├── urls.py
├── ai_assistant.py        # AI помощник для задач
├── ai_reports.py          # AI отчеты
├── ai.py                  # AI функции
├── tests.py
├── migrations/
│   ├── 0001_initial.py
│   └── 0002_taskattachment_taskcomment_taskhistory_tasklabel_and_more.py
└── templates/
    └── tasks/
        ├── task_list.html
        └── task_card.html
```

**Назначение**: Jira-подобная система управления задачами с AI-помощником.

**Модели**:
- `Task`: Основная модель задачи
- `TaskLabel`: Метки/теги для задач
- `TaskComment`: Комментарии к задачам
- `TaskAttachment`: Вложения файлов
- `TaskHistory`: История изменений

**AI функции**:
- Анализ задач
- Улучшение описаний
- Разбивка на подзадачи
- Генерация отчетов

---

### `/passwords` - Django приложение: Пароль-менеджер

```
passwords/
├── __init__.py
├── admin.py
├── apps.py
├── models.py              # Модели (PasswordEntry)
├── views.py               # Представления
├── urls.py
├── encryption.py          # Шифрование паролей
├── tests.py
├── migrations/
│   └── 0001_initial.py
└── templates/
    └── passwords/
        └── list.html
```

**Назначение**: Безопасное хранение и управление паролями с шифрованием AES-256.

**Функции**:
- Шифрование паролей
- Категории и теги
- Генератор паролей
- Аудит доступа

---

### `/servers` - Django приложение: Управление серверами

```
servers/
├── __init__.py
├── admin.py
├── apps.py
├── models.py              # Модели (Server, ServerGroup)
├── views.py               # Представления
├── urls.py
├── tests.py
├── migrations/
│   ├── 0001_initial.py
│   └── 0002_servergroup_advanced.py
└── templates/
    └── servers/
        └── list.html
```

**Назначение**: Управление SSH серверами, выполнение команд, группировка серверов.

**Модели**:
- `Server`: SSH сервер
- `ServerGroup`: Группы серверов

**Функции**:
- Добавление/редактирование серверов
- Тестирование подключений
- Выполнение команд
- История команд
- Шифрование паролей

---

### `/web_ui` - Настройки Django проекта

```
web_ui/
├── __init__.py
├── settings.py            # Настройки Django
├── urls.py                # Корневые URL маршруты
├── wsgi.py                # WSGI конфигурация
└── asgi.py                # ASGI конфигурация (для Daphne)
```

**Назначение**: Конфигурация Django проекта.

**Основные настройки** (`settings.py`):
- `INSTALLED_APPS`: Установленные приложения
- `DATABASES`: Конфигурация базы данных
- `MEDIA_ROOT`, `STATIC_URL`: Медиа и статические файлы
- `AGENT_PROJECTS_DIR`: Директория для проектов агентов
- `CLI_RUNTIME_CONFIG`: Конфигурация CLI рантаймов

---

### `/docs` - Документация

```
docs/
├── ARCHITECTURE.md        # Архитектура системы выбора моделей
├── MODEL_SELECTION.md     # Руководство по выбору моделей
└── UI_GUIDE.md            # Визуальное руководство по UI
```

**Назначение**: Техническая документация проекта.

---

### `/media` - Медиа файлы

```
media/
├── uploads/               # Загруженные пользователями файлы
└── workflows/             # Workflow файлы агентов
    ├── workflow-1.json
    ├── workflow-1.ralph.yml
    ├── workflow-2.json
    └── ...
```

**Назначение**: Хранение загруженных файлов и workflow конфигураций.

---

## 🔄 Потоки данных

### Запрос пользователя → Ответ AI

```
1. Пользователь отправляет сообщение в чат (core_ui/views.py)
   ↓
2. Orchestrator.process_user_message() (app/core/orchestrator.py)
   ↓
3. RAG Engine поиск релевантных документов (app/rag/engine.py)
   ↓
4. LLM Provider генерация ответа (app/core/llm.py)
   ↓
5. Стриминг ответа обратно пользователю
```

### Выполнение агента

```
1. API запрос на /api/agents/execute/ (core_ui/views.py)
   ↓
2. AgentManager получение агента (app/agents/manager.py)
   ↓
3. Agent.execute() выполнение задачи (app/agents/*.py)
   ↓
4. Использование инструментов при необходимости (app/tools/*.py)
   ↓
5. Возврат результата
```

### Загрузка файла

```
1. Пользователь загружает файл (core_ui/views.py)
   ↓
2. FileProcessor обработка файла (app/utils/file_processor.py)
   ↓
3. Извлечение текста (PDF, DOCX, OCR для изображений)
   ↓
4. Добавление в RAG Engine (app/rag/engine.py)
   ↓
5. Индексация в векторной БД (Qdrant или InMemory)
```

---

## 🗄️ База данных

### Основные таблицы

- **auth_user**: Пользователи Django
- **agent_hub_agentprofile**: Профили агентов
- **agent_hub_agentpreset**: Пресеты агентов
- **agent_hub_agentworkflow**: Workflow агентов
- **agent_hub_agentrunlog**: Логи выполнения
- **tasks_task**: Задачи
- **tasks_tasklabel**: Метки задач
- **tasks_taskcomment**: Комментарии
- **tasks_taskattachment**: Вложения
- **passwords_passwordentry**: Записи паролей
- **servers_server**: Серверы
- **servers_servergroup**: Группы серверов

### Миграции

Все миграции находятся в соответствующих директориях `*/migrations/`. Применение:

```bash
python manage.py migrate
```

---

## 🔧 Конфигурационные файлы

### `.env` - Переменные окружения

```env
GEMINI_API_KEY=...
GROK_API_KEY=...
DJANGO_PORT=9000
NICEGUI_PORT=8080
CLI_RUNTIME_TIMEOUT_SECONDS=600
```

### `.model_config.json` - Конфигурация моделей

```json
{
  "chat_model_gemini": "gemini-2.0-flash-exp",
  "agent_model_gemini": "gemini-2.0-flash-exp",
  "chat_model_grok": "grok-beta",
  "agent_model_grok": "grok-beta",
  "rag_model": "sentence-transformers/all-MiniLM-L6-v2",
  "default_provider": "gemini"
}
```

### `docker-compose.yml` - Docker Compose

Конфигурация для запуска Qdrant векторной БД.

---

## 📝 Важные замечания

1. **База данных**: По умолчанию используется SQLite (`db.sqlite3`). Для продакшена рекомендуется PostgreSQL.

2. **Медиа файлы**: Хранятся в `media/` директории. Убедитесь, что директория существует и доступна для записи.

3. **Агент проекты**: Проекты агентов хранятся в `agent_projects/` (создается автоматически).

4. **Конфигурация**: `.env` и `.model_config.json` не должны попадать в репозиторий (указаны в `.gitignore`).

5. **Зависимости**: Некоторые зависимости опциональны (OCR, PDF, DOCX). Система работает без них с ограниченной функциональностью.

---

## 🔍 Поиск по проекту

### Где находится логика агентов?
→ `/app/agents/`

### Где находится RAG система?
→ `/app/rag/`

### Где находятся инструменты агентов?
→ `/app/tools/`

### Где находится веб-интерфейс?
→ `/core_ui/` (Django) и `/app/ui/` (NiceGUI)

### Где находятся модели данных?
→ В соответствующих Django приложениях: `*/models.py`

### Где находится конфигурация?
→ `/web_ui/settings.py` и `.env` файл

---

Этот документ должен помочь быстро ориентироваться в структуре проекта и находить нужные компоненты.
