# WEU MVP — мини-сборка для тестов: без PyTorch, без моделей RAG, без тяжёлых зависимостей.
# Чат, задачи, пароли, серверы, оркестратор — работают. RAG и эмбеддинги — только в full.

# БД
psycopg[binary]>=3.1.8

# LLM
google-genai
httpx
aiohttp
python-dotenv
loguru
pydantic

# Агенты и инструменты
paramiko
asyncssh
mcp
beautifulsoup4
aiofiles

# Web
django
daphne
channels
channels-redis

# Task Queue
celery[redis]>=5.3.0
redis>=5.0.0

# Qdrant client (нужен для импорта RAGEngine; RAG-эмбеддинги только в full)
qdrant-client

# Файлы (лёгкие; OCR/DOCX/расширенный PDF — в full)
Pillow
PyPDF2
cryptography
numpy

# Dev tools (linting, testing)
ruff>=0.3.0
pytest>=8.0.0
pytest-django>=4.5.0
pytest-asyncio>=0.23.0
pre-commit>=3.6.0
