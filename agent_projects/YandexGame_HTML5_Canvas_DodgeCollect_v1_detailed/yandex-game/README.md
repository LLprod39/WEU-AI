# yandex-game

Vite + TypeScript (vanilla-ts).

## Команды

```bash
# Установка зависимостей
npm install

# Режим разработки
npm run dev

# Сборка
npm run build

# Сборка и упаковка (создаёт dist.zip в корне проекта)
npm run pack

# Превью собранного приложения
npm run preview
```
