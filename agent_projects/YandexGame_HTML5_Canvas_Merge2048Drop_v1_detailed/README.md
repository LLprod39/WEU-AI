# Yandex Merge 2048 Drop

## Команды

```bash
npm i
npm run dev
npm run build
npm run pack
```

- **npm i** — установка зависимостей
- **npm run dev** — запуск dev-сервера
- **npm run build** — сборка в папку `dist`
- **npm run pack** — сборка и упаковка `dist` в `dist.zip`
