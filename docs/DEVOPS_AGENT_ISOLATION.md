# Изоляция DevOps-агентов

## Проблема

По умолчанию агенты (Cursor/Claude CLI) могут искать и читать файлы родительского проекта `web_rA`, даже если у них указан изолированный `workspace`. Это проблема для серверных (DevOps) задач, которые должны работать ТОЛЬКО с SSH серверами через MCP, без доступа к коду платформы.

## Решение

Система автоматически применяет **полную изоляцию** для задач, помеченных как серверные.

### Когда активируется изоляция

Изоляция включается автоматически если:

1. **Серверная задача** - у workflow указан `target_server_id` (выбран целевой сервер)
2. **Явная изоляция** - в `workflow.script` указано `workspace_mode: "empty"`
3. **Ограничение файлов** - в `workflow.script` указано `restrict_files: true`

### Что делает изоляция

1. **Пустой workspace** - создается временная изолированная директория без кода проекта
2. **Sandbox режим** - передается `--sandbox enabled` в Cursor CLI для ограничения filesystem
3. **Read-only режим** - убирается флаг `--force`, агент не может изменять файлы локально
4. **.cursorignore** - блокирует доступ ко ВСЕМ файлам через индексацию Cursor
5. **.cursorrules** - правила для агента с явными запретами
6. **Системный промпт** - агенту передаются четкие инструкции:
   - Запрет на поиск файлов (Glob, Find, SemanticSearch)
   - Запрет на чтение файлов проекта (Read, Cat)
   - Запрет на подъем по директориям (`../`)
   - Разрешены только MCP инструменты: `server_execute`, `servers_list`
7. **README в workspace** - создается файл с инструкциями для агента

### Пример использования

#### Вариант 1: Автоматическая изоляция (рекомендуется)

При создании workflow выберите целевой сервер в поле `target_server`:

```json
{
  "name": "Deploy Redis",
  "target_server_id": 123,
  "script": {
    "steps": [
      {
        "title": "Развернуть Redis в Docker",
        "prompt": "Установи Redis через docker compose"
      }
    ]
  }
}
```

#### Вариант 2: Явная изоляция

Укажите `workspace_mode: "empty"` в script:

```json
{
  "name": "Server Maintenance",
  "script": {
    "workspace_mode": "empty",
    "steps": [
      {
        "title": "Update packages",
        "prompt": "Обнови все пакеты на сервере"
      }
    ]
  }
}
```

#### Вариант 3: Ограничение файлов

```json
{
  "name": "Config Update",
  "script": {
    "restrict_files": true,
    "steps": [
      {
        "title": "Update nginx config",
        "prompt": "Обнови конфиг nginx на сервере"
      }
    ]
  }
}
```

### Системный промпт для изолированных задач

При активации изоляции агент получает следующий системный промпт:

```
=== РЕЖИМ DEVOPS-АГЕНТА: ИЗОЛИРОВАННАЯ СРЕДА ===

РАБОЧАЯ ДИРЕКТОРИЯ: /tmp/weu_server_task_xxxxx
Это пустая изолированная директория. Здесь НЕТ кода проекта.

ВЫ РАБОТАЕТЕ КАК DEVOPS-АГЕНТ для управления удаленными серверами через SSH.

СТРОГО ЗАПРЕЩЕНО:
❌ Искать файлы (Glob, Find, ls -R, find)
❌ Читать файлы проекта (Read, cat, head, tail)
❌ Подниматься вверх по директориям (../, ../../)
❌ Анализировать структуру проекта
❌ Использовать SemanticSearch, Grep для поиска кода

ДОСТУПНЫЕ ИНСТРУМЕНТЫ:
✅ mcp__weu-servers__server_execute - выполнить команду на удаленном сервере
✅ mcp__weu-servers__servers_list - список доступных SSH серверов
✅ Bash - ТОЛЬКО для локальных вспомогательных команд (не для поиска файлов!)

КАК РАБОТАТЬ:
1. Сначала вызови servers_list чтобы увидеть доступные серверы
2. Используй server_execute для выполнения команд на сервере
3. Все команды Docker, systemd, apt, yum - через server_execute
4. Локальный Bash - только для создания скриптов, если нужно

ЕСЛИ ЗАДАЧА ТРЕБУЕТ РАБОТЫ С КОДОМ:
Откажись и объясни: "Я DevOps-агент, работаю только с серверами. Для работы с кодом используйте агента разработки."
```

### Доступные MCP инструменты

В изолированном режиме агент имеет доступ к:

#### `servers_list`
Получить список доступных SSH серверов пользователя.

**Пример:**
```
servers_list()
```

**Ответ:**
```json
{
  "servers": [
    {
      "id": 1,
      "name": "Production Server",
      "host": "prod.example.com",
      "username": "deploy"
    }
  ]
}
```

#### `server_execute`
Выполнить команду на удаленном сервере через SSH.

**Параметры:**
- `server_id` (int) - ID сервера из servers_list
- `command` (string) - команда для выполнения
- `timeout` (int, optional) - таймаут в секундах (по умолчанию 300)

**Пример:**
```
server_execute(
  server_id=1,
  command="docker compose up -d redis",
  timeout=120
)
```

**Ответ:**
```json
{
  "success": true,
  "stdout": "Container redis started",
  "stderr": "",
  "exit_code": 0
}
```

### Настройка в CLI_RUNTIME_CONFIG

Изоляция использует следующие параметры Cursor CLI:

```python
CLI_RUNTIME_CONFIG = {
    "cursor": {
        "allowed_args": ["model", "sandbox", "approve-mcps", "browser", "allowedTools"],
        ...
    }
}
```

При изоляции автоматически передается:
- `--sandbox enabled` - ограничение filesystem
- `--allowedTools "Bash,mcp__weu-servers__server_execute,mcp__weu-servers__servers_list"` - только разрешенные инструменты

### Проверка изоляции

В логах выполнения workflow вы увидите:

```
[Isolation enabled: server_task, sandbox=enabled]
[Workspace restriction: using temp dir]
```

Если агент все еще пытается искать файлы - это будет видно в логах как:
```
🔍 Поиск файлов: docker-compose.yml
📁 Листинг: ../../web_rA
```

В этом случае проверьте:
1. Указан ли `target_server_id` или `workspace_mode: "empty"`
2. Передается ли параметр `is_server_task=True` в `_run_steps_with_backend`
3. Применяется ли системный промпт (смотрите `meta.step_X_prompt` в Admin Logs)

### Отладка

Если изоляция не работает:

1. **Проверьте логи workflow**:
   ```
   [Isolation enabled: ...]
   [Workspace restriction: using temp dir]
   ```

2. **Проверьте команду CLI** (Admin Logs):
   ```json
   {
     "step_1_cmd": "agent -p --force --sandbox enabled --workspace /tmp/weu_server_xxxxx ..."
   }
   ```

3. **Проверьте промпт** (Admin Logs):
   ```json
   {
     "step_1_prompt": "=== РЕЖИМ DEVOPS-АГЕНТА: ИЗОЛИРОВАННАЯ СРЕДА ===\n\n..."
   }
   ```

### Ограничения

- Sandbox режим не поддерживается Claude CLI (только Cursor)
- Агент все еще может игнорировать системный промпт (зависит от модели)
- Некоторые модели могут быть более "упрямыми" и продолжать искать файлы

### Альтернативные решения

Если изоляция через промпт не работает:

1. **Используйте отдельный Docker контейнер** для агента
2. **Запускайте агента с ограниченными правами filesystem** (chroot, namespaces)
3. **Используйте whitelist режим** с пустым `allowed_paths`

## История изменений

- **2026-02-02**: Добавлена полная изоляция для серверных задач
  - Системный промпт с запретами
  - Sandbox режим
  - README в workspace
  - Параметр `is_server_task` в `_run_steps_with_backend`
