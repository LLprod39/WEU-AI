# Руководство по выбору моделей

## Обзор

Теперь вы можете выбирать конкретные модели для разных задач в вашем AI агенте. Это позволяет оптимизировать производительность и стоимость для каждого типа задачи.

## Где выбирать модели

### 1. В интерфейсе чата
В верхней панели чата вы найдете:
- **Модель**: **Авто** (по умолчанию) — ответы через Cursor CLI в режиме Ask (только чтение/ответы, без изменений); **Gemini** или **Grok** — облачные модели
- **Конкретная модель**: для Gemini/Grok — выбор варианта (Flash/Pro и т.п.)
- **RAG**: Включение/выключение базы знаний

При выборе **Авто** запросы уходят в Cursor CLI: Ask — `agent --mode=ask -p "..." --output-format text --workspace ...`; Agent (правка файлов) — `agent -p --force --output-format stream-json --stream-partial-output --workspace ... --model auto "..."` (у CLI нет `--mode=agent`, только plan/ask). Режим настраивается в **Настройки → Models → «Режим Cursor CLI в чате»**. Выбранная модель используется для всех сообщений в этом чате.

### 2. В настройках (⚙️)
Откройте настройки для детальной конфигурации:

#### Вкладка "Модели"
Здесь вы можете настроить модели для разных целей:

**Gemini Models:**
- 💬 **Модель для чата**: Используется для обычных диалогов
- 🤖 **Модель для агента**: Используется для выполнения задач с инструментами (ReAct)

**Grok Models:**
- 💬 **Модель для чата**: Используется для обычных диалогов
- 🤖 **Модель для агента**: Используется для выполнения задач с инструментами (ReAct)

**RAG / Embeddings:**
- 📚 **Модель для RAG**: Используется для векторизации текста
  - Gemini embeddings: Требуют API ключ, высокое качество
  - SentenceTransformers: Работает локально, бесплатно

**Провайдер по умолчанию:**
- Используется при запуске приложения

## Рекомендации по выбору моделей

### Для чата
- **Gemini 2.0 Flash**: Быстрая, хорошо подходит для диалогов
- **Gemini 1.5 Pro**: Более мощная, лучше для сложных задач
- **Grok 4**: Отличное качество, хорошая скорость

### Для агентов (ReAct)
- **Gemini 2.0 Flash Exp**: Экспериментальная, быстрая
- **Grok 4**: Отлично справляется с инструментами
- Рекомендуется использовать более мощные модели для агентов

### Для RAG
- **paraphrase-multilingual-MiniLM-L12-v2**: Локальная, бесплатная, хорошее качество
- **text-embedding-004**: Gemini embeddings, требует API ключ, высокое качество

## Как это работает

1. **В чате**: Выбранная модель используется напрямую для генерации ответов
2. **В агентах**: Модель используется для ReAct цикла (рассуждение + действие)
3. **В RAG**: Модель используется для создания векторных представлений текста

## Сохранение настроек

Все настройки автоматически сохраняются в файл `.model_config.json` и `.env` (для API ключей).

## Обновление списка моделей

Нажмите кнопку 🔄 "Обновить список моделей" в настройках для получения актуального списка доступных моделей от провайдеров.

## Примеры использования

### Сценарий 1: Экономия средств
- Чат: Gemini 1.5 Flash 8B (быстрая и дешевая)
- Агент: Gemini 1.5 Pro (мощная для сложных задач)
- RAG: SentenceTransformers (бесплатная)

### Сценарий 2: Максимальное качество
- Чат: Grok 4
- Агент: Grok 4
- RAG: Gemini text-embedding-004

### Сценарий 3: Быстрая работа
- Чат: Gemini 2.0 Flash
- Агент: Gemini 2.0 Flash Exp
- RAG: SentenceTransformers
