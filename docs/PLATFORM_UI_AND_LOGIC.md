# WEU AI — интерфейс, кнопки и логика работы

Документ дополняет [FEATURES.md](FEATURES.md): для каждой области платформы описана **подробная логика работы** (без кода, стрелками и текстом) и **каждая кнопка/элемент интерфейса** — что делает, с чем связан, куда ведёт.

**Версия:** 2.0.0 — DevOps/IT Edition

---

## Оглавление

1. [Общая навигация и шапка](#1-общая-навигация-и-шапка)
2. [Dashboard (главная)](#2-dashboard-главная)
3. [Чат](#3-чат)
4. [Задачи (Tasks)](#4-задачи-tasks)
5. [Серверы](#5-серверы)
6. [Агенты и воркфлоу](#6-агенты-и-воркфлоу)
7. [База знаний (RAG)](#7-база-знаний-rag)
8. [Настройки и доступ](#8-настройки-и-доступ)
9. [Пароли](#9-пароли)
10. [Оркестратор, IDE, Monitor](#10-оркестратор-ide-monitor)
11. [ИИ: инструкции, контекст и ожидаемый вывод](#11-ии-инструкции-контекст-и-ожидаемый-вывод)

---

## 1. Общая навигация и шапка

### Где находится

- **Боковая панель (sidebar)** — слева на всех страницах.
- **Шапка (header)** — сверху: хлебные крошки, уведомления, справка, статус, модель.

### Логика появления пунктов меню

```
Права пользователя (permissions)
    → user_can_servers  → показывается "Servers"
    → user_can_tasks    → показывается "Tasks"
    → user_can_agents   → показываются "Monitor", "Agents", "Workflows"
    → user_can_orchestrator → показывается "IDE"
    → user_can_knowledge_base → показывается "Knowledge"
    → user_can_settings → показывается "Settings"
```

Пункты **Chat**, **Passwords** и **Sign Out** доступны всем авторизованным.

### Кнопки и ссылки (по порядку)

| Элемент | Действие | Связи |
|--------|-----------|--------|
| **Логотип / WEU AI** | Визуальный бренд, клик никуда не ведёт | — |
| **Collapse sidebar** (chevron) | Сворачивает/разворачивает боковую панель | Состояние хранится визуально |
| **Dashboard** | Переход на главную (`/`) | Статистика, быстрые действия, последние запуски |
| **Monitor** | Страница мониторинга (`/monitor/`) | Запуски агентов, статусы (если есть право на агенты) |
| **Servers** | Список SSH-серверов (`/servers/`) | Группы, карточки серверов, терминал |
| **Tasks** | Список задач (`/tasks/`) | Проекты, доска, бэклог |
| **Agents** | Agent Hub (`/agents/`) | Профили, воркфлоу, запуски |
| **Workflows** | Тот же Agent Hub с параметром `?legacy=1` | Legacy-вид воркфлоу |
| **Chat** | Чат с AI (`/chat/`) | Сессии, модели, RAG |
| **IDE** | Просмотр/редактирование файлов (`/ide/`) | Файловая система (если есть право) |
| **Knowledge** | База знаний RAG (`/knowledge-base/`) | Документы, поиск |
| **Settings** | Настройки приложения (`/settings/`) | Ключи, модели, доступ (если есть право) |
| **Passwords** | Менеджер паролей (`/passwords/`) | Учётные записи, категории |
| **Sign Out** | POST на `/logout/` | Выход из системы |

### Шапка (header)

| Элемент | Действие | Связи |
|--------|-----------|--------|
| **Mobile menu** | На мобильных открывает боковое меню | Тот же список, что в sidebar |
| **Breadcrumbs** | Home / Текущая страница | Навигация назад на главную |
| **Колокольчик (Уведомления)** | Открывает выпадающий список уведомлений | Задачи: приглашения, делегирование ИИ, назначение, упоминания; кнопки действий внутри уведомлений |
| **Help** | Открывает правую панель справки | Статьи (RAG, Workflow, модель, серверы), кнопка «Повторить тур» |
| **Feedback** | Ссылка на GitHub Issues | Внешний ресурс |
| **Status** | Показывает состояние подключения | Бейдж онлайн/офлайн |
| **Текущая модель** | Текст модели (например Gemini). Клик → переход в Settings (если есть право) | Блок настроек моделей |

---

## 2. Dashboard (главная)

**URL:** `/` или `/dashboard/`

### Как это работает

```
Загрузка страницы
    → Запрос api/dashboard/stats/ (серверы, задачи, агенты, воркфлоу)
    → Отображение четырёх карточек статистики
    → Блок «Quick Actions» и «Recent Agent Runs» заполняются из контекста/API
```

Стрелка «что куда ведёт»:

- **Active Servers** → `/servers/`
- **Pending Tasks** → `/tasks/`
- **Running Agents** → `/monitor/`
- **Workflows** → `/agents/`

### Кнопки и ссылки

| Элемент | Действие | Связи |
|--------|-----------|--------|
| **Карточка «Active Servers»** (стрелка) | Переход на список серверов | `servers:server_list` |
| **Карточка «Pending Tasks»** (стрелка) | Переход на список задач | `tasks:task_list` |
| **Карточка «Running Agents»** (стрелка) | Переход на Monitor | `monitor` |
| **Карточка «Workflows»** (стрелка) | Переход на Agent Hub | `agent_hub:agents_page` |
| **Quick: Run Agent** | Переход на Agents | Запуск агента вручную на той странице |
| **Quick: New Task** | Переход на Tasks с `?action=new` | Открытие формы создания задачи |
| **Quick: Add Server** | Переход на Servers | Добавление сервера на той странице |
| **Quick: AI Chat** | Переход на Chat | Новая сессия чата |
| **Recent runs — View All** | Переход на Monitor | Список всех запусков |
| **Элемент в списке Recent runs** | Переход к конкретному run (если есть ссылка) | Логи/детали запуска |

---

## 3. Чат

**URL:** `/chat/`

### Как это работает (общая логика)

```
Пользователь вводит сообщение
    → Выбор: провайдер (Smart Auto / Cursor / Gemini / Grok), модель, режим (Chat / ReAct), RAG вкл/выкл
    → Отправка на api/chat/ (стриминг)
    → Ответ приходит по кускам → отображается в реальном времени
```

Режимы:

- **Chat (Fast):** один запрос к LLM, без цепочки инструментов.
- **ReAct (Precise):** цикл «мысль → действие инструментом → наблюдение» до финального ответа.

Если включён **RAG:**

```
Запрос пользователя
    → Эмбеддинг запроса → поиск по базе знаний (api/rag/query или внутренний вызов)
    → Найденные фрагменты подмешиваются в контекст LLM
    → Ответ учитывает документы из базы
```

Если в URL передан **task_id** (например `/chat/?task_id=123`):

```
Загрузка чата
    → В панели над полем ввода показывается контекст задачи (название, описание, статус)
    → Сообщения могут учитывать эту задачу (передаётся в API)
    → Ссылка «К задачам» ведёт на /tasks/#task-123
```

### Кнопки и элементы чата

| Элемент | Действие | Связи |
|--------|-----------|--------|
| **Открыть панель чатов** (таб справа) | Показывает список чатов, если панель была свёрнута | api/chats/, история сессий |
| **Новый** (в панели чатов) | Создаёт новый чат (api/chats/new/), переключает на него | Текущая сессия сбрасывается в UI |
| **Свернуть панель** (chevron) | Скрывает боковую панель с историей чатов | — |
| **Поиск по чатам** | Фильтрация списка чатов по введённому тексту | Локально по списку |
| **Элемент в списке чатов** | Переключение на выбранную сессию, подгрузка сообщений | api/chats/<id>/ |
| **Быстрые карточки (Сводка задач, Анализ файла, Статус серверов)** | Подставляют готовый промпт в поле ввода | Только вставка текста |
| **Поле ввода + Send** | Отправка сообщения на api/chat/ | Стриминг ответа, при необходимости RAG и инструменты |
| **Прикрепление файлов** | Загрузка через api/chat/upload/, файлы попадают в контекст LLM | Модель «видит» содержимое |
| **Провайдер (Smart Auto / Cursor / Gemini / Grok)** | Выбор бэкенда для ответа | api/models/, настройки в Settings |
| **Модель** (выпадающий список) | Конкретная модель выбранного провайдера | api/models/ |
| **RAG** (чекбокс) | Включение/выключение поиска по базе знаний перед ответом | knowledge-base, api/rag/query |
| **Mode (Chat / ReAct)** | Режим: быстрый ответ или цикл с инструментами | unified_orchestrator, режимы в app/core/modes/ |
| **Очистить историю** (иконка) | Очистка текущей сессии в UI (и/или api/clear-history/) | Текущий чат |
| **Панель «Обсуждаемая задача»** | Показ при task_id в URL, ссылка на задачу | /tasks/#task-<id> |

В сообщениях от ассистента могут быть интерактивные блоки (например **Task Board**): кнопки «Создать задачу», «Обсудить», «Делегировать ИИ» — они ведут к созданию/редактированию задачи или делегированию и связаны с `tasks` и уведомлениями.

---

## 4. Задачи (Tasks)

**URL:** `/tasks/`, проекты `/tasks/projects/`, спринты, бэклог и т.д.

### Логика списка задач и доски

```
Вход на /tasks/
    → Выбор проекта (sidebar): «All» или конкретный проект
    → Загрузка задач по проекту (фильтры, доска)
    → Задачи отображаются в колонках по статусу (To Do, In Progress, Blocked, Done)
```

Перетаскивание карточки:

```
Перетаскивание карточки в другую колонку
    → POST /tasks/<id>/update-status/ с новым status
    → Обновление счётчиков колонок в UI
```

### Логика модального окна задачи (Detail Modal)

```
Клик по карточке задачи → openDetailModal(taskId)
    → GET /tasks/<id>/ (task_detail_api) → данные задачи, подзадачи, комментарии
    → Открытие модалки: описание, подзадачи, комментарии, статус, приоритет, сервер, время
```

Действия внутри модалки:

- **Изменить статус** (select) → POST `update-status/`.
- **Изменить приоритет** (select) → POST `update-priority/`.
- **Изменить сервер** (select) → POST `update-server/`.
- **Improve** → POST `ai-improve/` → AI переписывает описание → подстановка в поле.
- **Breakdown** → POST `ai-breakdown/` → AI возвращает подзадачи → добавление в список подзадач.
- **Execute** → переход к делегированию ИИ (форма/подтверждение) или approve auto-execution.
- **Discuss** → переход в чат с `?task_id=<id>`.
- **Добавить подзадачу** (поле + Add) → POST `subtask/`.
- **Чекбокс подзадачи** → POST `subtask/<id>/toggle/`.
- **Комментарий** (текст + Comment) → POST `comment/`.

### Кнопки и ссылки (Tasks)

| Элемент | Действие | Связи |
|--------|-----------|--------|
| **Выбор проекта** (sidebar) | Переключение контекста на «All» или проект | project_list, текущий список задач |
| **New Task** (или аналог) | Открытие модалки создания задачи | createModal, POST при сохранении |
| **New Project** | Модалка нового проекта | project_create |
| **New Sprint** | Модалка нового спринта (в контексте проекта) | sprint_create |
| **Фильтры / сохранённый вид** | Применение фильтра или «Save current view» | filter_save, filter_list |
| **Карточка задачи** (клик по области) | Открытие модалки деталей | openDetailModal, task_detail_api |
| **На карточке: иконка чата** | Переход в чат с этой задачей | /chat/?task_id=<id> |
| **На карточке: удалить** | Подтверждение и DELETE задачи | task_delete |
| **В модалке: Improve** | AI улучшает описание | ai-improve |
| **В модалке: Breakdown** | AI разбивает на подзадачи | ai-breakdown |
| **В модалке: Execute** | Запуск/делегирование ИИ | delegate_form, approve_auto_execution |
| **В модалке: Discuss** | Переход в чат с task_id | /chat/?task_id= |
| **В модалке: Status / Priority / Server** (select) | Обновление поля задачи | update-status, update-priority, update-server |
| **В модалке: Add (подзадача)** | Создание подзадачи | subtask_create |
| **В модалке: Comment** | Добавление комментария | comment_create |
| **Bulk-действия** (если есть) | Массовое изменение выбранных задач | tasks_bulk_action |

### Проекты, участники, спринты, материалы

- **Проект → Настройки** → страница настроек проекта (ключ, участники, материалы).
- **Приглашение в проект** → invite → письмо/ссылка → invitation_respond.
- **Материалы проекта** → добавление/удаление документов, ссылок.
- **Спринты** → создание, старт, завершение, добавление задач в спринт.

Логика не меняется: каждая кнопка ведёт на соответствующий URL из `tasks/urls.py` (project_views, team_views, views).

---

## 5. Серверы

**URL:** `/servers/`, терминал `/servers/<id>/terminal/`, hub `/servers/hub/`.

### Как это работает

```
Список серверов
    → Группировка по группам (ServerGroup), у пользователя — только разрешённые серверы
    → Карточка: имя, хост, порт, статус (тест подключения), действия
```

Подключение и команды:

```
Клик «Terminal» по серверу
    → Открывается страница с WebSocket-терминалом
    → Установка SSH-сессии → ввод команд, вывод в браузере
```

```
Кнопка «Execute» (если есть)
    → Ввод команды → POST api/<id>/execute/ → проверка safety → выполнение на сервере → вывод результата
```

Группы:

```
Создание/редактирование группы
    → api/groups/create/ или update/ → добавление/удаление участников, подписка (subscribe)
```

### Кнопки и элементы (Servers)

| Элемент | Действие | Связи |
|--------|-----------|--------|
| **Добавить сервер** | Открытие формы/модалки создания | server_create |
| **Карточка сервера: Terminal** | Переход на страницу терминала этого сервера | server_terminal, WebSocket |
| **Карточка сервера: Execute / Test** | Выполнение команды или проверка подключения | server_execute, server_test |
| **Карточка сервера: Редактирование** | Изменение хоста, порта, учётки | server_update |
| **Hub (Multi-terminal)** | Несколько терминалов в одной вкладке | multi_terminal |
| **Группа: создать / изменить / удалить** | Управление группой и участниками | group_create, group_update, group_delete, add/remove-member, subscribe |

В терминале могут быть кнопки **AI** (запрос к AI по контексту, предложение команд с подтверждением) — они связаны с WebSocket-сообщениями ai_request, ai_confirm, ai_cancel.

---

## 6. Агенты и воркфлоу

**URL:** `/agents/` (Agent Hub).

### Как это работает (кратко)

```
Quick start (текст задачи + «Создать и запустить»)
    → Выбор рантайма (Cursor CLI / Ralph), проекта (существующий или новый)
    → autoCreateAll(): создание воркфлоу/проекта при необходимости → запуск run
    → Редирект или обновление статуса, отображение в «Active runs» и в логах
```

```
Запуск существующего воркфлоу
    → Выбор воркфлоу из списка → Run → api/workflows/run/
    → Статус через api/workflows/run/<run_id>/status/, при необходимости Stop / Restart / Skip step / Retry / Continue
```

```
Builder
    → Пошаговое создание/редактирование воркфлоу (шаги, параметры) → сохранение
```

```
Import
    → Загрузка JSON воркфлоу → api/workflows/import/
```

Связь с задачами:

- **Воркфлоу из задачи** (api/workflows/from-task/) — по выбранной задаче генерируется воркфлоу.
- Делегирование задачи ИИ (из Tasks) создаёт/запускает сценарий и связано с уведомлениями (AUTO_EXECUTION_SUGGESTION и т.д.).

### Кнопки и элементы (Agents)

| Элемент | Действие | Связи |
|--------|-----------|--------|
| **Quick: поле ввода задачи** | Текст для auto-create | — |
| **Quick: Runtime (Cursor / Ralph)** | Выбор среды выполнения | CLI runtime config |
| **Quick: Проект** | Выбор папки/проекта или «Новый проект» | api/projects/ |
| **Создать и запустить** | autoCreateAll(): воркфлоу + run | api/workflows/run/, api/run/ |
| **Builder** | Открытие конструктора воркфлоу | Task builder, api/workflows/ |
| **Import** | Модалка загрузки JSON | api/workflows/import/ |
| **В списке воркфлоу: Run** | Запуск выбранного воркфлоу | api/workflows/run/ |
| **В списке воркфлоу: Редактирование / Удаление** | Изменение или удаление воркфлоу | api/workflows/<id>/update/, delete/ |
| **Active run: Stop / Restart / Skip / Retry / Continue** | Управление текущим прогоном | api/workflows/run/<id>/stop/, restart/, skip/, retry/, continue/ |
| **Logs** | Переход на страницу логов | logs_page, api/runs/ |
| **Custom Agents** | Страница кастомных агентов | custom_agents_view, api/custom-agents/ |
| **MCP (если есть в UI)** | Список/подключение MCP-серверов | api/mcp/servers/, connect, disconnect, tools |

---

## 7. База знаний (RAG)

**URL:** `/knowledge-base/`.

### Как это работает

```
Загрузка документа
    → Текст отправляется на api/rag/add/
    → Разбиение на чанки, эмбеддинги (если full build), сохранение в Qdrant или InMemory
    → Счётчик документов обновляется
```

```
Поиск
    → Ввод запроса → api/rag/query/ (или вызов из UI)
    → Эмбеддинг запроса → поиск по коллекции → вывод совпадений
```

В чате при включённом RAG тот же поиск используется для подмешивания контекста в запрос к LLM.

### Кнопки и элементы (Knowledge Base)

| Элемент | Действие | Связи |
|--------|-----------|--------|
| **Поле поиска + Search** | Семантический поиск по базе | api/rag/query/, searchKnowledge() |
| **Загрузка документа** (если есть форма) | Добавление текста/файла в RAG | api/rag/add/ |
| **Список документов** | Просмотр загруженных (api/rag/documents/) | api/rag/delete/ при удалении |
| **Reset / Clear** | Очистка коллекции | api/rag/reset/ |
| **Счётчик docs, RAG active/offline** | Информация о состоянии | api/rag/documents/, конфиг RAG (mini/full) |

---

## 8. Настройки и доступ

**URL:** `/settings/`, `/settings/access/`, `/settings/users/`, `/settings/groups/`, `/settings/permissions/`.

### Как это работает

```
Settings (главная)
    → Загрузка api/settings/ → отображение ключей (статус из .env), моделей, провайдера
    → Изменение настроек → сохранение через api/settings/
```

```
Доступ (Access)
    → Пользователи: список, смена пароля (api/access/users/, .../password/)
    → Группы: список, участники (api/access/groups/, .../members/)
    → Permissions: права на задачи, серверы, воркфлоу (api/access/permissions/)
```

### Кнопки и элементы (Settings)

| Элемент | Действие | Связи |
|--------|-----------|--------|
| **API Keys (статусы)** | Только отображение (настроено в .env) | api/settings/check/ или контекст |
| **Провайдер / модели (Chat, Agent, RAG)** | Выбор и сохранение | api/settings/ |
| **Disk** | Отображение использования диска | api/disk/ |
| **Settings → Access** | Переход к управлению доступом | settings_access_view |
| **Users: смена пароля** | Форма смены пароля пользователя | api_access_user_password |
| **Groups: участники** | Добавление/удаление участников группы | api_access_group_members |
| **Permissions: создать/редактировать** | Назначение прав на объекты | api_access_permissions |

Настройки **проекта** (ключ, участники, материалы): страница `settings_project` (project_settings) — те же идеи: кнопки ведут на project_views (members, invite, materials).

---

## 9. Пароли

**URL:** `/passwords/`.

### Как это работает

```
Список учётных записей
    → Фильтрация по категории/тегам, поиск по имени/логину/email/URL
    → Клик по записи → расшифровка пароля (api/<id>/decrypt/) по MASTER_PASSWORD
```

Создание: форма → api/create/. Генерация пароля: api/generate-password/.

### Кнопки и элементы (Passwords)

| Элемент | Действие | Связи |
|--------|-----------|--------|
| **Добавить запись** | Форма новой учётной записи | credential_create |
| **Показать пароль** (decrypt) | Запрос расшифровки для отображения | credential_decrypt |
| **Генератор пароля** | Генерация случайного пароля | generate_password |
| **Категории / теги** | Фильтр списка | Модели CredentialCategory, CredentialTag |

---

## 10. Оркестратор, IDE, Monitor

### Orchestrator

**URL:** упоминается в FEATURES; если в приложении есть отдельная страница `/orchestrator/` — то это просмотр инструментов (Tools), доступных агенту в ReAct.

- **Refresh** — обновление списка инструментов (api/tools/ или аналог).
- **Поиск / фильтр по категориям** — фильтрация списка инструментов в UI.
- Карточки инструментов — только отображение (название, описание, категория). Запуск инструментов происходит внутри чата в режиме ReAct, а не кнопками на этой странице.

### IDE

**URL:** `/ide/`.

- Список файлов: api/ide/files/.
- Открытие/редактирование файла: api/ide/file/ (GET/POST).
- Кнопки «Сохранить» и т.п. вызывают запись через тот же API.

### Monitor

**URL:** `/monitor/`.

- Отображение активных и недавних запусков агентов/воркфлоу.
- Ссылки на детали run, логи (agent_hub: admin_api_runs_list, status, update, restart и т.д.).

---

## 11. ИИ: инструкции, контекст и ожидаемый вывод

Ниже описано **какие инструкции получает ИИ** при каждом сценарии, **какой контекст** ему передаётся и **что он должен вывести**. Это нужно, чтобы понимать поведение ассистента при нажатии кнопок и при работе в чате/задачах.

---

### 11.1. Режим «Чат» (Chat Mode)

**Когда срабатывает:** в чате выбран режим «Chat (Fast)» и пользователь отправил сообщение.

**Что передаётся в ИИ (системный промпт):**

1. **Системные правила (CHAT_SYSTEM_RULES):**
   - Профессиональный тон, без emoji.
   - Для задач — инструменты: `tasks_list`, `task_detail`, `task_create`, `task_update`, `task_delete`.
   - Формат ответов: markdown, таблицы, списки, badges.
   - Один вызов инструмента на запрос.
   - Ссылки на задачи: `[#ID](task:ID)`.
   - Badges: `[STATUS]` `[PRIORITY]`.
   - Если вопрос про срочность/сроки — вызывать `tasks_list` с `sort_by="urgency"`, `include_completed=false`.
   - Удалять задачу через `task_delete` только при явном запросе и с `confirm=true`.

2. **Описание доступных инструментов** — список инструментов и их параметров (из Tool Manager).

3. **Контекст пользователя** — при наличии: `User ID: <id>`.

4. **Контекст задачи** — если в чат зашли с `?task_id=...` (или передан `task_context_id` в запросе):
   - Текущая задача: название, статус, due_date, описание (до 1000 символов).
   - Инструкция: «Если пользователь спрашивает про "эту/ту задачу" — отвечай про эту задачу. Не делай сводку всех задач, если не просили список.»

5. **История** — последние 4 сообщения (роль + до 150 символов контента).

6. **База знаний (RAG)** — при включённом RAG: до 2 релевантных фрагментов из поиска по запросу.

7. **Формат ответа:**  
   - Если нужны данные — одна строка: `ACTION: tool_name {"param": "value"}`.  
   - Если данные не нужны — сразу ответ пользователю.

8. **Запрос пользователя** — текст сообщения.

**Что должен вывести ИИ:**

- Либо **обычный текст** (ответ пользователю).
- Либо **строка ACTION** — тогда бэкенд выполняет инструмент и делает **второй запрос** к ИИ с результатом инструмента (см. «Финальный промпт» ниже).

**Финальный промпт (после вызова инструмента):**

- Для **tasks_list** при запросе-сводке: «Сделай краткую сводку: первая строка — "Сводка: всего N задач (активных: X, завершённых: Y)", затем блоки по статусам, в каждой строке — название, приоритет, исполнитель, срок. Русский, без emoji.»
- Для **tasks_list** при другом запросе: «Ответь по сути вопроса, используя данные задач. Если задач несколько — выбери самую релевантную и укажи какую. Практический ответ, не сводка списком.»
- Для **task_detail**: «Дай конкретный ответ по этой задаче: если спрашивают "как выполнить" — пошаговый план; учитывай статус, приоритет, срок, описание; при нехватке данных — короткие уточняющие вопросы; не делай общую сводку.»
- Для остальных инструментов: «Отформатируй данные для пользователя на русском.»

**Специальный вывод «доска задач» (Task Board):**

- Если был вызван `tasks_list` и запрос распознан как «сводка/список/активные/просроченные» и т.п., бэкенд **не** просит ИИ сформировать текст сводки во втором запросе, а сам собирает из ответа инструмента структурированный JSON (**WEU_TASKS_JSON**) и отдаёт его в поток. В сообщении в чате появляется блок с карточками задач и кнопками (Создать задачу, Обсудить, Делегировать ИИ и т.д.). Структура payload: `type: "task_board"`, `tasks`, `summary` (total, returned, active, completed, has_more, offset, limit), `source_tool`, `query`.

---

### 11.2. Режим «ReAct» (ReAct Mode)

**Когда срабатывает:** в чате выбран режим «ReAct (Precise)» и пользователь отправил сообщение.

**Что передаётся в ИИ (системный промпт):**

1. **Роль:** «You are WEU Agent — интеллектуальный ассистент с доступом к инструментам.»

2. **AGENT_SYSTEM_RULES_RU** (сводно):
   - Язык: только русский.
   - Не запрашивать пароли; для SSH — только переданный `connection_id` или инструменты `servers_list` / `server_execute`.
   - Если не хватает данных — задать 1–2 вопроса и не делать ACTION.
   - Код: аккуратно, проверка границ, без лишних TODO.
   - Серверы: сначала `servers_list`, затем `server_execute` по имени/ID.
   - Разрешено: чтение, проверка (df, логи, status, конфиги). Запрещено без подтверждения: rm, mkfs, отключение сервисов и т.п.
   - Задачи: `tasks_list`, `task_detail`; при запросе сводки — обязательно вызвать `tasks_list`.
   - Формат вывода задач: первая строка «Сводка: всего N задач…», заголовки `### STATUS (N)`, ссылки `**[#ID](task:ID)**`, без emoji.

3. **Контекст выполнения (execution_context)** — если задача делегирована ИИ и уже есть SSH-соединение:
   - «Уже установлено SSH-соединение с сервером: &lt;имя&gt; (&lt;хост&gt;).»
   - «Используй ТОЛЬКО инструмент ssh_execute с параметром conn_id=&lt;id&gt;. НЕ вызывай ssh_connect, servers_list, server_execute.»
   - «Разрешённые действия: &lt;allowed_actions&gt;.»
   - При этом из списка инструментов **исключаются**: `ssh_connect`, `servers_list`, `server_execute`.

4. **Блок «Твои серверы»** — если передан `user_id` и нет делегированного connection_id: список серверов (имя, id, user@host:port) и подсказка использовать `server_execute` с `server_name_or_id`.

5. **Рабочая директория (workspace_path)** — если открыт чат из IDE: путь к проекту; «все пути к файлам указывай относительно этой директории».

6. **Режим IDE (from_ide)** — если True: «НЕ выводи данные серверов (хосты, пароли, команды ssh). Фокусируйся только на коде.» Список серверов не подмешивается.

7. **Доступные инструменты** — полное описание (с учётом exclude_tools при делегировании).

8. **База знаний** — либо релевантные фрагменты (до 3), либо «Нет релевантного контекста.»

9. **История диалога** — последние 6 сообщений (роль + усечённый контент; OBSERVATION до 3000 символов).

10. **Инструкции по формату:**
    - Рассуждай по шагам на русском.
    - Если нужен инструмент: строго  
      `THOUGHT: <рассуждение>`  
      `ACTION: tool_name {"param1": "value1", ...}`
    - После OBSERVATION продолжать рассуждение или дать итоговый ответ.
    - Итоговый ответ — без строки ACTION.

11. **Запрос пользователя.**

**Что должен вывести ИИ:**

- В каждой итерации: либо **THOUGHT + ACTION** (тогда выполняется инструмент, результат подставляется как OBSERVATION и цикл повторяется), либо **итоговый ответ текстом** без ACTION.
- В конце ReAct может вызываться **шаг верификации**: ИИ получает «ИСХОДНЫЙ ЗАПРОС», «ИСПОЛЬЗОВАННЫЕ ИНСТРУМЕНТЫ», «ТВОЙ ОТВЕТ» и вопросы проверки. Должен ответить либо `VERIFIED: OK`, либо `IMPROVE: <описание>`.
- Если вызывались `tasks_list`/`task_detail`, в итоговый текст подставляются ссылки вида `**[#ID](task:ID)**`. Если был сформирован task_board payload — к ответу дописывается строка `WEU_TASKS_JSON:<json>` для отображения карточек в UI.

---

### 11.3. Режим «Ralph Internal»

**Когда срабатывает:** выбран режим Ralph (внутренний итеративный агент), например из конфига оркестратора или при запуске делегированной задачи с типом ralph.

**Первая итерация — что передаётся:**

- «You are working on the following task. Work on it step by step.»
- «Task: &lt;сообщение пользователя&gt;»
- При включённом RAG: блок «📚 &lt;фрагменты&gt;».
- «When you complete the task, output exactly: &lt;promise&gt;&lt;completion_promise&gt;&lt;/promise&gt;»
- «CRITICAL RULE: Do NOT output the promise unless it is completely and unequivocally TRUE.»
- «If requirements are unclear, list 1–3 clarifying questions and state your assumptions.»
- Подсказка при блокировке: «Если задача заблокирована, опиши блокеры и что нужно для прогресса. Не выводи promise, если работа не завершена.»

**Следующие итерации:**

- «Continue working on this task. You've completed N iteration(s).»
- «Original Task: …»
- «Previous Results: &lt;результат предыдущей итерации&gt;»
- «Review your previous work, identify what needs improvement, and continue.»
- Те же правила про promise и уточняющие вопросы.

**Что должен вывести ИИ:**

- Обычный текст по шагам.
- Завершение: строка `<promise><completion_promise></promise>` — тогда цикл останавливается. Иначе — следующая итерация до лимита.

---

### 11.4. Задачи: кнопка «Improve» (улучшить описание)

**Когда срабатывает:** в модалке задачи нажали «Improve».

**Что передаётся в ИИ:**

- «You are a professional project manager. Improve the following task description to be more clear, actionable, and professional.»
- «Task Title: &lt;название&gt;»
- «Current Description: &lt;текущее описание&gt;»
- «Return only the improved description as plain text. Do not add any conversational filler.»

**Что должен вывести ИИ:** один текст — улучшенное описание задачи (без пояснений и разметки). Подставляется в поле описания в модалке.

---

### 11.5. Задачи: кнопка «Breakdown» (разбить на подзадачи)

**Когда срабатывает:** в модалке задачи нажали «Breakdown».

**Что передаётся в ИИ:**

- «You are a professional project manager. Break down the following task into smaller, actionable subtasks.»
- «Task Title: …», «Description: …»
- «Return the subtasks as a JSON list of strings. Example: ["Subtask 1", "Subtask 2"]. Do not add markdown or other text.»

**Что должен вывести ИИ:** JSON-массив строк, например `["Подзадача 1", "Подзадача 2"]`. Допускается обёртка в markdown (```json ... ```). При ошибке парсинга — разбор по строкам (нумерация снимается). Результат добавляется в список подзадач в модалке.

---

### 11.6. Задачи: анализ задачи (Smart Analyzer) и делегирование ИИ

**Когда срабатывает:** создание/сохранение задачи с включённым умным анализом или переход к делегированию (например из уведомления «Делегировать ИИ»).

**Что передаётся в ИИ (анализатор):**

- Роль: «Ты ИИ-анализатор задач. Определи, может ли ИИ автоматически выполнить задачу на основе ТОЛЬКО имеющихся данных.»
- «ЗАДАЧА: &lt;название&gt;», «ОПИСАНИЕ: &lt;описание&gt;»
- **Доступные серверы пользователя:** список вида «- &lt;имя&gt; (хост: …, порт: …)» или «Список доступных серверов пуст».
- **Созданные агенты пользователя:** для каждого — ID, имя, описание, инструкции (первые 200 символов), инструменты, доступ к серверам, runtime; или «У пользователя нет созданных агентов…».
- Блок «Твои возможности» (подключение SSH, установка пакетов, проверка статуса/логов, конфиги, скрипты) и «Ограничения» (удаление критичных данных, отключение production, пароли — нужно подтверждение).
- Строгие правила: определить упоминание сервера из списка; проверить подходящего агента; при отсутствии уверенности — `can_delegate_to_ai: false` и указать reason/missing_info; при опасной задаче — не предлагать авто-выполнение.

**Что должен вывести ИИ:** строго JSON с полями:

- `can_delegate_to_ai` (bool)
- `target_server_name` (имя из списка или null)
- `recommended_custom_agent_id` (int или null)
- `recommended_agent` ("react"|"simple"|"complex"|"ralph")
- `reason` (обоснование на русском)
- `missing_info`, `estimated_time`, `complexity`, `risks`

По этому ответу создаются уведомления (предложение выполнения, подтверждение сервера, вопросы, отказ с причиной) и при делегировании выбирается агент и сервер.

---

### 11.7. Делегированная задача: что получает ИИ при выполнении на сервере

**Когда срабатывает:** пользователь подтвердил выполнение задачи ИИ (например из уведомления), бэкенд подключился к серверу по SSH и запускает агента (ReAct/Ralph).

**Что передаётся в execution_context:**

- `connection_id` — идентификатор уже установленного SSH-соединения (пароли и хосты ИИ не видит).
- `server`: `{ "name": "<имя сервера>", "host": "<хост>" }` — для отображения в промпте.
- `allowed_actions` — например «выполнение команд на сервере» или «readonly, проверка (df, логи, статус)».
- `user_id`, при необходимости `task_id`.

В системный промпт ReAct подставляется блок «КОНТЕКСТ ВЫПОЛНЕНИЯ ЗАДАЧИ» и исключаются инструменты `ssh_connect`, `servers_list`, `server_execute`. ИИ может вызывать только `ssh_execute` с этим `conn_id`.

**Что должен вывести ИИ:** пошаговое выполнение задачи на сервере (THOUGHT + ACTION с ssh_execute), затем итоговый отчёт. Результат пишется в выполнение задачи (TaskExecution) и отображается в уведомлениях/логах.

---

### 11.8. Smart Router (сложность задачи для выбора runtime)

**Когда срабатывает:** при необходимости автоматически выбрать режим/агента для задачи (например при генерации воркфлоу или умном запуске).

**Что передаётся в ИИ:**

- «Analyze DevOps/IT task complexity:»
- «Title: …», «Description: …» (описание до 500 символов)
- «Context: Files count, Config size, Server»

**Что должен вывести ИИ:** только JSON с полями: `requires_multi_file_coordination`, `config_files_size_kb`, `is_infrastructure_change`, `requires_deep_reasoning`, `is_quick_fix`, `estimated_time_minutes`, `subtasks_count`, `complexity`, `risk_level`. По нему выбирается orchestrator_mode, runtime, model (с учётом доступных провайдеров).

---

### 11.9. Чат с контекстом задачи (task_id в URL)

**Когда срабатывает:** открыт чат с `?task_id=<id>` или в запросе api/chat передан `task_context_id`.

**Что дополнительно передаётся в ИИ:**

- В **execution_context** попадает **task_context**: id, title, description (до 1000 символов), status, priority, due_date, assignee.
- В промпте чата (Chat/ReAct) добавляется блок **«КОНТЕКСТ ЗАДАЧИ»** / **«TASK CONTEXT»**: те же поля плюс инструкция «If user asks about 'this task', refer to this context instead of listing all tasks.»

Таким образом, при вопросах вроде «что делать по этой задаче» или «как выполнить» ИИ ориентируется на одну задачу, а не на полный список.

---

### 11.10. Сводка: откуда ИИ получает данные при нажатии кнопок

| Действие пользователя | Откуда контекст | Что ИИ должен вывести |
|------------------------|-----------------|------------------------|
| Отправил сообщение в чате (Chat) | Правила чата + инструменты + история + RAG + опционально task_context | Текст или ACTION → затем финальный ответ или WEU_TASKS_JSON |
| Отправил сообщение в чате (ReAct) | AGENT_SYSTEM_RULES_RU + инструменты + RAG + история + execution_context | THOUGHT/ACTION или итог; опционально верификация и task board JSON |
| Открыл чат по задаче (?task_id=) | task_context в промпте и execution_context | Ответы в контексте одной задачи |
| Improve описание задачи | Заголовок + описание | Улучшенный текст описания |
| Breakdown задачи | Заголовок + описание | JSON-массив строк подзадач |
| Анализ задачи (делегирование) | Задача + серверы пользователя + кастомные агенты | JSON: can_delegate_to_ai, reason, recommended_agent, target_server_name и др. |
| Выполнение делегированной задачи на сервере | connection_id + server + allowed_actions, без ssh_connect/servers_list/server_execute | Шаги ssh_execute и итоговый отчёт |
| Smart Router | Заголовок + описание + контекст (files, config, server) | JSON сложности и риска для выбора runtime |

---

## Сводная таблица «кнопка → API/страница»

| Область | Типичные действия | Endpoint или страница |
|---------|-------------------|------------------------|
| Чат | Отправить сообщение, новый чат, очистить | api/chat/, api/chats/new/, api/clear-history/ |
| Чат | Загрузка файла | api/chat/upload/ |
| Задачи | Создать, обновить статус/приоритет/сервер, удалить | task_create, update-status, update-priority, update-server, task_delete |
| Задачи | AI: улучшить, разбить, делегировать | ai-improve, ai-breakdown, delegate_form, approve_auto_execution |
| Задачи | Подзадачи, комментарии | subtask_create, subtask_toggle, comment_create |
| Задачи | Уведомления: действия из выпадающего списка | notifications_list, notification_action |
| Серверы | Создать, обновить, выполнить команду, терминал | server_create, server_update, server_execute, server_terminal |
| Агенты | Запуск воркфлоу, stop/restart/skip/retry | api/workflows/run/, run/<id>/stop/, restart/, skip/, retry/, continue/ |
| Агенты | Импорт, создание из задачи | api/workflows/import/, api/workflows/from-task/ |
| RAG | Добавить, поиск, сброс, документы | api/rag/add/, query/, reset/, documents/ |
| Настройки | Сохранить настройки, доступ | api/settings/, api/access/* |
| Пароли | Создать, расшифровать, сгенерировать пароль | credential_create, credential_decrypt, generate_password |

---

Этот файл можно использовать вместе с [FEATURES.md](FEATURES.md) для онбординга и справочника по интерфейсу и логике платформы без погружения в код.
